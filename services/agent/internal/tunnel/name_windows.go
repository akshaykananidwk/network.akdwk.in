//go:build windows

package tunnel

// defaultInterfaceName is what appears in Network Connections, so it is the
// product name rather than a device-style abbreviation.
const defaultInterfaceName = "AKConnect"
